import { AxiosError } from "axios";
declare const isAxiosError: <T = unknown, D = unknown>(e: unknown) => e is AxiosError<T, D>;
interface IHttpRequestParameters {
    baseUri: string;
    apiKey: string;
}
interface IHttpClientParameters extends IHttpRequestParameters {
    apiClientName: string;
}
declare class SOOSApiClient {
    private static createHttpClient;
    static create(params: IHttpClientParameters): import("axios").AxiosInstance;
}
export { isAxiosError };
export default SOOSApiClient;
